# reveal-md basic usage

In order to serve the slide decks, use [reveal-md](https://www.npmjs.com/package/reveal-md)
The easiest way to get going is to install it via npm [(download NodeJS)](https://nodejs.org/en/)  
```
npm install -g reveal-md
```

and then

```
reveal-md example.md
```